// import { useSessionStore } from "../session/session.store";

// export function getEntryRoute(): string {
//     const { isAuthenticated } = useSessionStore.getState();

//     return isAuthenticated ? "/dashboard" : "/login";
// }
